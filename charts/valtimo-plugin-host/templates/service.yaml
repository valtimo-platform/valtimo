apiVersion: v1
kind: Service
metadata:
  name: {{ include "valtimo-plugin-host.fullname" . }}
  labels:
    {{- include "valtimo-plugin-host.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valtimo-plugin-host.selectorLabels" . | nindent 4 }}
