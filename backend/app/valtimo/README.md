# Valtimo (backend)

The base **Valtimo** application — the process-automation platform without the
GZAC case-management additions. Released as `ritense/valtimo-backend`.

## How to start

With the supporting services already running, from the repository root:

```bash
./gradlew :backend:app:valtimo:bootRun
```
