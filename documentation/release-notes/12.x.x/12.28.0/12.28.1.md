# 12.28.1

## Bugfixes

* Fixed an issue in forms where conditions on fields that were contained in a Data Grid type field were not working
  correctly.

* Case documents are now properly displayed again when `valtimo.authorization.zgwDocuments.enabled` is set to `false`
